#include <iostream>
#include <vector>

using namespace std;


int helper(vector<int> v, int n)
{
    int ans= 0;

    

  
  return ans;
};

int main()
{
    int N;

    cin >> N;

    vector<int> A(N);

    for (int i = 0; i < N; i++)
    {
        cin >> A[i];
    }
    
    return helper(A,N);

}
